<UserControl x:Class="Just.WPF.Views.MongoDBTool.MongoDBToolCtrl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Just.WPF.Views.MongoDBTool"
             xmlns:cvt="clr-namespace:Just.WPF.Converters"
             xmlns:local1="clr-namespace:Just.WPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Unloaded="UserControl_Unloaded">
    <UserControl.Resources>
        <cvt:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <cvt:BoolReverseConverter x:Key="BoolReverseConverter"/>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <local1:WaitLayer Grid.RowSpan="99" Visibility="{Binding Doing,Converter={StaticResource BoolToVisibilityConverter}}" Panel.ZIndex="{Binding Doing}" />

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition/>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock VerticalAlignment="Center">脚本目录:</TextBlock>
            <TextBox Grid.Column="1" Margin="5,0,0,0" Text="{Binding JsonFolder}"></TextBox>
            <Button Grid.Column="2" Margin="5,0,0,0" Command="{Binding JsonFolderBrowser}">...</Button>
            <Button Grid.Column="3" Margin="5,0,0,0" Style="{StaticResource MainButton}" Command="{Binding DoAction}">合并</Button>
        </Grid>
        <Grid Grid.Row="1" Margin="0,5,0,0" Visibility="{Binding ShowJs,Converter={StaticResource BoolToVisibilityConverter}}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" VerticalAlignment="Center">执行脚本:</TextBlock>
            <TextBox Grid.Column="1" Margin="5,0,0,0" Text="{Binding JsFile}"></TextBox>
            <Button Grid.Column="2" Margin="5,0,0,0" Command="{Binding JsFileBrowser}">...</Button>
        </Grid>
        <Grid Grid.Row="2" Margin="0,5,0,0" Visibility="{Binding ShowJs,Converter={StaticResource BoolToVisibilityConverter}}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" VerticalAlignment="Center">替换正则:</TextBlock>
            <TextBox Grid.Column="1" Margin="5,0,0,0" Text="{Binding Pattern}"></TextBox>
            <TextBox Grid.Column="2" Margin="5,0,0,0" Text="{Binding Replacement}"></TextBox>
        </Grid>
        <Grid Grid.Row="9" Margin="0,5,0,0">
            <TextBox VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Text="{Binding Json}"/>
            <Button Style="{StaticResource GreenButton}" Margin="5" VerticalAlignment="Top" HorizontalAlignment="Right" Command="{Binding CopyJson}">复制</Button>
        </Grid>
    </Grid>
</UserControl>
