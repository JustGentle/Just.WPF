<UserControl x:Class="Just.WPF.Views.MongoDBTool.MongoDBToolCtrl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Just.WPF.Views.MongoDBTool"
             xmlns:cvt="clr-namespace:Just.WPF.Converters"
             xmlns:local1="clr-namespace:Just.WPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Unloaded="UserControl_Unloaded">
    <UserControl.Resources>
        <cvt:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <cvt:BoolReverseConverter x:Key="BoolReverseConverter"/>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <local1:WaitLayer Grid.RowSpan="2" Visibility="{Binding Doing,Converter={StaticResource BoolToVisibilityConverter}}" Panel.ZIndex="{Binding Doing}" />
        <Grid Grid.Row="0" Margin="0,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <TextBlock Margin="5,0" VerticalAlignment="Center">脚本目录:</TextBlock>
            <TextBox Grid.Column="1" Margin="5,0,0,0" Text="{Binding JsonFolder}"></TextBox>
            <Button Grid.Column="2" Margin="5,0,0,0" Command="{Binding JsonFolderBrowser}">...</Button>

            <TextBlock Grid.Row="1" Margin="5,0" VerticalAlignment="Center">执行脚本:</TextBlock>
            <TextBox Grid.Row="1" Grid.Column="1" Margin="5,5,0,0" Text="{Binding JsFile}"></TextBox>
            <Button Grid.Row="1" Grid.Column="2" Margin="5,5,0,0" Command="{Binding JsFileBrowser}">...</Button>

            <TextBlock Grid.Row="2" Margin="5,0" VerticalAlignment="Center">替换正则:</TextBlock>
            <Grid Grid.Row="2" Grid.Column="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" Margin="5,5,0,0" Text="{Binding Pattern}"></TextBox>
                <TextBox Grid.Column="1" Margin="5,5,0,0" Text="{Binding Replacement}"></TextBox>
            </Grid>
            

            <Button Grid.Row="3" Grid.Column="3" Margin="5,5,0,0" Style="{StaticResource BlueButton}" Command="{Binding DoAction}">合并</Button>
        </Grid>
        <TextBox Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Text="{Binding Json}"/>
    </Grid>
</UserControl>
